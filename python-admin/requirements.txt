# NetaTrack India — Python Admin requirements
# MySQL driver removed — now uses HTTP API only

requests>=2.31.0          # HTTP API calls (replaces mysql-connector-python)
python-dotenv>=1.0.0      # Load .env config
rich>=13.7.0              # Terminal UI (tables, progress, colors)
click>=8.1.7              # CLI commands
schedule>=1.2.1           # Cron-like job scheduler
google-generativeai>=0.5.0  # Gemini AI for auto-scoring
Pillow>=10.0.0
transformers>=4.40.0
