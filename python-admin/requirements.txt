mysql-connector-python>=8.3.0
requests>=2.31.0
# Local AI (pretrained models — downloads ~500MB on first run)
transformers>=4.40.0
torch>=2.2.0          # CPU-only is fine
# torch-cpu alternative (smaller): pip install torch --index-url https://download.pytorch.org/whl/cpu
