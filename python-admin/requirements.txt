# NetaTrack India — Python Admin App
# No C++ / No Visual Studio / No gcc needed
# Install: pip install -r requirements.txt

mysql-connector-python>=8.3.0
requests>=2.31.0
Pillow>=10.0.0
transformers>=4.40.0
# CPU-only PyTorch (no CUDA, no GPU, no C++ build tools)
# Install separately (pip install -r does not support --index-url):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# OR use onnxruntime instead of torch (even smaller, ~15MB):
#   pip install onnxruntime
