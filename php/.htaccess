# Block direct access to php/ app root
# Document root should be php/public/ not php/
# This file is a safety fallback only.

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteRule ^ public/index.php [QSA,L]
</IfModule>
